# Force pnpm to run build scripts for native modules
side-effects-cache=false
# Hoist dependencies to help with native module compilation
shamefully-hoist=true
# Always rebuild native modules on install
enable-pre-post-scripts=true
